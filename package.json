{
  "name": "claude-deepseek-bridge",
  "version": "0.3.0",
  "description": "Let Claude Code and Codex delegate bounded tasks to local DeepSeek Harness: shared skills, a supervised launcher (dsb), and a session viewer (dsv).",
  "private": true,
  "license": "LGPL-2.1-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AAAA9731/claude-deepseek-bridge.git"
  },
  "type": "module",
  "bin": {
    "dsv": "bin/dsv.mjs",
    "dsb": "bin/dsb.mjs"
  },
  "files": [
    "bin",
    "lib",
    "skill",
    "scripts"
  ],
  "engines": {
    "node": ">=23.8"
  },
  "scripts": {
    "check": "node --check bin/dsv.mjs && node --check bin/dsb.mjs && node --check lib/bridge.mjs && node --check lib/sessions.mjs"
  }
}
